---
- name: Configure platform hosts
  hosts: platform
  become: true
  tasks:
    - name: Ensure required packages are present
      ansible.builtin.package:
        name:
          - curl
          - ca-certificates
        state: present
